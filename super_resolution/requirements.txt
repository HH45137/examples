# 核心依赖
torch
torchvision
numpy
Pillow
six

# 替代 OpenCV 的 GPU 友好依赖 (仅需额外安装这两个包)
imageio          # 视频文件 I/O (FFmpeg 后端)
imageio-ffmpeg   # FFmpeg 二进制插件 (支持 NVDEC GPU 硬件解码)

# ONNX Runtime 推理后端 (选择其一)
# onnxruntime           # CPU 推理
# onnxruntime-gpu       # GPU (CUDA) 推理
# onnxruntime-directml  # DirectML (AMD/Intel GPU Windows)

# ONNX 导出与 INT8 量化
onnx                 # ONNX 模型格式支持
onnxscript           # PyTorch 2.11+ ONNX 导出所需
